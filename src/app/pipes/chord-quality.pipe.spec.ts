import { ChordQualityPipe } from './chord-quality.pipe';

describe('ChordQualityPipe', () => {
  it('create an instance', () => {
    const pipe = new ChordQualityPipe();
    expect(pipe).toBeTruthy();
  });
});
