import { ChordNamePipePipe } from './chord-name-pipe.pipe';

describe('ChordNamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChordNamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
