import { AddModuleDirective } from './add-module.directive';

describe('AddModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddModuleDirective();
    expect(directive).toBeTruthy();
  });
});
